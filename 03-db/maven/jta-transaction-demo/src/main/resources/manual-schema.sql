

create table `spring1`.`employee` (
    id int,
    first_name varchar(32),
    last_name varchar(32),
    email varchar(32),
    phone_number varchar(32),
    hire_date date,
    salary int
);

create table `spring2`.`product` (
    id int,
    name varchar(32),
    quantity int,
    price float,
    available boolean
);

